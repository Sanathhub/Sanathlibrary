@Library('mylibrary02')_
pipeline
{   agent  any
    stages
    {   stage(contdownload)
        {   steps
            {   script
                { check.contd()
                }
            }
        }
    }
}
